> npm init{
    package name: (procurement_manager)
    version: (1.0.0)
    description: App to resize image dimensions
    entry point: (index.js) main.js
    test command:
    git repository:
    keywords:
    author: Alexangelo Andrews
    liscense: (ISC) MIT
}

(next, add this to package.json file = {
    "productName": Procurement_Manager
})

(next, under "scripts" in package.json add a start script = {
    "start": "electron ."
})

>npm i electron

- add functionality to the select company option
- add search functionality to the browse suppliers package
- add search functionality to the add company page